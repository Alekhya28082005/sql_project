CREATE PROCEDURE add_doctor(
    did VARCHAR(20),
    dname VARCHAR(30),
    d_speciality VARCHAR(20),
    d_years int
) 
BEGIN
    INSERT INTO Patient VALUES (pid, pname, page, paddress, doctor_id);
END;
